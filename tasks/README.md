# Tasks

Codex task handoff files live here.

Use kebab-case filenames, for example:

```text
inspect-example-repo.md
fix-auth-regression.md
add-health-check-endpoint.md
```

Each task should define goal, context, working directory, allowed files, validation, expected output, and result file.
